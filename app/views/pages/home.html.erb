<div class='jumbotron'>
  <h1><i class="fa fa-file-text" aria-hidden="true"></i> Spacr</h1>
  <p>An app that corrects English text so that it contains exactly one space after a period between sentences.</p>

</div class='form-control'>
  <%= form_tag('api/v1/texts', id: 'text-form', remote:true) do %>
    <%= text_area_tag :text, params[:text], :required => 'required', class: 'form-control', id: 'text-area', :rows => 10 %>
    <%= submit_tag 'Submit your text', class: 'btn btn-primary btn-lg btn-block' %></div>
  <% end %>
</div>
